# Third-party notices

GENERATED — run `node tools/build-icons.mjs <path-to-game-icons>` to refresh.

Everything in Aetheria Cartographer is original work under the MIT licence,
with one exception: 182 of the map symbols are built from vector
outlines published on [game-icons.net](https://game-icons.net).

## game-icons.net

**Licence:** [Creative Commons Attribution 3.0 Unported (CC BY 3.0)](https://creativecommons.org/licenses/by/3.0/)

**Source:** https://github.com/game-icons/icons

Only the path geometry is used. It is scaled, recoloured and composited into
medallions and shields by `src/assets/procedural/icons.ts`; the upstream files
themselves are not redistributed. Each icon keeps its author in the asset's
tags and in the application's About box, and the licence is satisfied by that
credit remaining visible.

If you redistribute maps that contain these symbols, keep the attribution with
them.

### delapouite — 84 icons

- Eagle Head (`eagle-head`)
- Bear Head (`bear-head`)
- Horse Head (`horse-head`)
- Unicorn (`unicorn`)
- Griffin Symbol (`griffin-symbol`)
- Fleur De Lys (`fleur-de-lys`)
- Oak Leaf (`oak-leaf`)
- Shamrock (`shamrock`)
- Tower Flag (`tower-flag`)
- Castle (`castle`)
- Gauntlet (`gauntlet`)
- Star Formation (`star-formation`)
- Falcon Moon (`falcon-moon`)
- Harp (`harp`)
- Castle Ruins (`castle-ruins`)
- Ancient Ruins (`ancient-ruins`)
- Village (`village`)
- Temple Gate (`temple-gate`)
- Greek Temple (`greek-temple`)
- Byzantin Temple (`byzantin-temple`)
- Egyptian Pyramids (`egyptian-pyramids`)
- Watchtower (`watchtower`)
- Lighthouse (`lighthouse`)
- Windmill (`windmill`)
- Barn (`barn`)
- Cave Entrance (`cave-entrance`)
- Mountain Cave (`mountain-cave`)
- Gold Mine (`gold-mine`)
- Mine Wagon (`mine-wagon`)
- Well (`well`)
- Arch Bridge (`arch-bridge`)
- Drawbridge (`drawbridge`)
- Harbor Dock (`harbor-dock`)
- Sailboat (`sailboat`)
- Wooden Pier (`wooden-pier`)
- Tavern Sign (`tavern-sign`)
- Shop (`shop`)
- Blacksmith (`blacksmith`)
- Stable (`stable`)
- Obelisk (`obelisk`)
- Dolmen (`dolmen`)
- Menhir (`menhir`)
- Graveyard (`graveyard`)
- Church (`church`)
- Tipi (`tipi`)
- Oasis (`oasis`)
- Olive (`olive`)
- Corn (`corn`)
- Carrot (`carrot`)
- Fish Bucket (`fish-bucket`)
- Wool (`wool`)
- Sheep (`sheep`)
- Cow (`cow`)
- Beehive (`beehive`)
- Gold Nuggets (`gold-nuggets`)
- Coal Wagon (`coal-wagon`)
- Wood Pile (`wood-pile`)
- Brick Pile (`brick-pile`)
- Amphora (`amphora`)
- Barrel (`barrel`)
- Coins (`coins`)
- Falling Rocks (`falling-rocks`)
- Swamp (`swamp`)
- Plague Doctor Profile (`plague-doctor-profile`)
- Kraken Tentacle (`kraken-tentacle`)
- Giant Squid (`giant-squid`)
- Bat (`bat`)
- Shark Fin (`shark-fin`)
- Whale Tail (`whale-tail`)
- Goblin Head (`goblin-head`)
- Orc Head (`orc-head`)
- Centaur (`centaur`)
- Swords Power (`swords-power`)
- Cavalry (`cavalry`)
- Siege Tower (`siege-tower`)
- Trebuchet (`trebuchet`)
- Shield Bash (`shield-bash`)
- Knight Banner (`knight-banner`)
- Gate (`gate`)
- Barracks Tent (`barracks-tent`)
- Cargo Crate (`cargo-crate`)
- Caravan (`caravan`)
- Walking Scout (`walking-scout`)
- War Pick (`war-pick`)

### lorc — 75 icons

- Lion (`lion`)
- Eagle Emblem (`eagle-emblem`)
- Wolf Head (`wolf-head`)
- Stag Head (`stag-head`)
- Raven (`raven`)
- Sea Serpent (`sea-serpent`)
- Rose (`rose`)
- Trefoil Lily (`trefoil-lily`)
- Sun (`sun`)
- Crescent Blade (`crescent-blade`)
- Crown (`crown`)
- Key (`key`)
- Chalice Drops (`chalice-drops`)
- Anvil (`anvil`)
- Broadsword (`broadsword`)
- Crossed Swords (`crossed-swords`)
- Axe In Stump (`axe-in-stump`)
- Spear Hook (`spear-hook`)
- Bowman (`bowman`)
- Moon (`moon`)
- Lightning Arc (`lightning-arc`)
- Mountains (`mountains`)
- Pine Tree (`pine-tree`)
- Wheat (`wheat`)
- Grapes (`grapes`)
- Boar Tusks (`boar-tusks`)
- Snake (`snake`)
- Anchor (`anchor`)
- Stone Tower (`stone-tower`)
- Guarded Tower (`guarded-tower`)
- Beer Stein (`beer-stein`)
- Tombstone (`tombstone`)
- Magic Portal (`magic-portal`)
- Campfire (`campfire`)
- Meat (`meat`)
- Cheese Wedge (`cheese-wedge`)
- Salt Shaker (`salt-shaker`)
- Chicken Leg (`chicken-leg`)
- Crystal Cluster (`crystal-cluster`)
- Gems (`gems`)
- Stone Block (`stone-block`)
- Knapsack (`knapsack`)
- Scales (`scales`)
- Hammer Nails (`hammer-nails`)
- Burning Embers (`burning-embers`)
- Poison Bottle (`poison-bottle`)
- Death Zone (`death-zone`)
- Spider Web (`spider-web`)
- Wolf Trap (`wolf-trap`)
- Mantrap (`mantrap`)
- Quicksand (`quicksand`)
- Thorn Helix (`thorn-helix`)
- Frozen Orb (`frozen-orb`)
- Ice Spear (`ice-spear`)
- Lightning Storm (`lightning-storm`)
- Tornado (`tornado`)
- Vortex (`vortex`)
- Magic Swirl (`magic-swirl`)
- Pentagram Rose (`pentagram-rose`)
- Triple Yin (`triple-yin`)
- Rune Stone (`rune-stone`)
- Crystal Ball (`crystal-ball`)
- Holy Symbol (`holy-symbol`)
- Cursed Star (`cursed-star`)
- Eclipse (`eclipse`)
- Fairy Wand (`fairy-wand`)
- Werewolf (`werewolf`)
- Direwolf (`direwolf`)
- Wolf Howl (`wolf-howl`)
- Scorpion (`scorpion`)
- Crow Dive (`crow-dive`)
- Minotaur (`minotaur`)
- Ghost (`ghost`)
- Flying Flag (`flying-flag`)
- Hunting Horn (`hunting-horn`)

### sbed — 6 icons

- Shield (`shield`)
- Water Drop (`water-drop`)
- Death Skull (`death-skull`)
- Spikes (`spikes`)
- Lava (`lava`)
- Helmet (`helmet`)

### skoll — 6 icons

- Pig (`pig`)
- Logging (`logging`)
- Evil Eyes (`evil-eyes`)
- Troll (`troll`)
- Skeleton (`skeleton`)
- Siege Ram (`siege-ram`)

### carl-olsen — 3 icons

- Flame (`flame`)
- Spider Alt (`spider-alt`)
- Crossbow (`crossbow`)

### caro-asercion — 3 icons

- Boar (`boar`)
- Medieval Village 01 (`medieval-village-01`)
- Water Mill (`water-mill`)

### faithtoken — 2 icons

- Dragon Head (`dragon-head`)
- Ore (`ore`)

### heavenly-dog — 1 icon

- Catapult (`catapult`)

### sparker — 1 icon

- Bear Face (`bear-face`)

### willdabeast — 1 icon

- Gold Bar (`gold-bar`)
